/**
 * Forms Components
 * Formulários específicos de domínios
 */

// Auth Forms
export { LoginForm } from '../auth/login-form'
export { RegisterForm } from '../auth/register-form'
export { OnboardingForm } from '../auth/onboarding-form'

// Other forms can be exported as added
